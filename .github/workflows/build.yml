name: Build ZMK firmware
on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Local copy of ZMK's v0.3 reusable workflow with Node 24 actions; see the file header.
    uses: ./.github/workflows/build-user-config.yml
